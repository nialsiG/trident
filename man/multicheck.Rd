% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{multicheck}
\alias{multicheck}
\title{multicheck}
\usage{
multicheck(df, y, alpha = 0.05)
}
\arguments{
\item{df}{A data frame composed entirely of numeric variables.}

\item{y}{A factor used as the grouping variable.}

\item{alpha}{Significance level (default: 0.05).}
}
\value{
A named list of p-values, ratios, and decision flags for each variable.
}
\description{
Perform various statistical checks on numeric variables against a grouping factor.
}
\details{
multicheck
}
\examples{
csv <- system.file("tests/csv/Vars.csv", package = "trident")

df <- read.csv(file = csv, header = TRUE, as.is = TRUE, row.names = NULL)

fa <- as.factor(c(rep("A", 15), rep("B", 15), rep("C", 15)))

df[] <- lapply(df, as.numeric)

DFstat <- multicheck(df = df, y = fa, alpha = 0.05)

csv <- system.file("tests/csv/DFstat.csv", package = "trident")

write.csv(DFstat, file = csv, row.names = FALSE, quote = FALSE)

}
